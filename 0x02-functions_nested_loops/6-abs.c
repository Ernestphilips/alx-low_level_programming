#include "main.h"

/**
 * main - write a function that computes the absolute value of an integer
 *
 * Return: 0 if successful
 */

int main(void); int _abs(int n)

{
	if (n < 0)
		n = -n;
	return (n);
}
