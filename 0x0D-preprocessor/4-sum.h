#ifndef SUM_H
#define SUM_H

/**
 * 4-sum.h
 * Desc: a function-like macro SUM(x, y) that computes the
 * sum of the numbers x and y
 * @x: parameter
 * @y: parameter
 */

#define SUM(x, y) ((x) + (y))

#endif
